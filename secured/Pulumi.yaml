name: gcp-workstations-secured
description: Secured GCP workstations setup
runtime:
  name: nodejs
  options:
    packagemanager: bun
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
